# Computer-Use Automation System

Planning repository for the interface.ai Staff Software Engineer take-home.

The implementation-ready technical plan is in [SPEC.md](./SPEC.md). Source code, the required `REPORT.md`, and redacted run evidence will be added during implementation.

## Assignment through-line

The model discovers. A typed artifact becomes a reusable capability. Production replay invokes that capability deterministically with no model in the decision loop.

